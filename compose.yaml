services:
  portfolio:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:?Set PUBLIC_ORIGIN to your HTTPS site origin}
    volumes:
      - journal-data:/app/data
volumes:
  journal-data:
